services:
  - type: web
    name: spotify-support-agent
    env: python
    plan: free
    buildCommand: pip install -r requirements-web.txt
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false   # set in Render dashboard, not stored in repo
    healthCheckPath: /
